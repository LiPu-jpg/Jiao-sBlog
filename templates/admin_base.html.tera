<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}后台管理{% endblock %}</title>
    <link rel="stylesheet" href="/static/style.css">
    <style>
        .admin-container {
            display: flex;
            min-height: 100vh;
            background: var(--background-color);
        }
        
        .admin-nav {
            width: 250px;
            background: var(--primary-color);
            color: white;
            padding: 30px 20px;
            box-shadow: var(--shadow-medium);
            position: fixed;
            height: 100vh;
            overflow-y: auto;
        }
        
        .admin-nav h2 {
            color: white;
            text-align: center;
            margin-bottom: 30px;
            border-left: none;
            padding-left: 0;
        }
        
        .admin-nav a {
            display: block;
            color: white;
            padding: 12px 16px;
            margin-bottom: 8px;
            text-decoration: none;
            border-radius: var(--border-radius);
            transition: var(--transition);
            font-weight: 500;
        }
        
        .admin-nav a:hover {
            background: rgba(255,255,255,0.1);
            text-decoration: none;
            transform: translateX(5px);
        }
        
        .admin-main {
            flex: 1;
            margin-left: 250px;
            padding: 40px;
            background: var(--background-color);
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            background: var(--surface-color);
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--shadow-light);
            margin-bottom: 30px;
        }
        
        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid var(--border-color);
        }
        
        th {
            background: var(--primary-color);
            color: white;
            font-weight: 600;
        }
        
        tr:hover {
            background: rgba(52, 152, 219, 0.05);
        }
        
        .action-buttons {
            display: flex;
            gap: 10px;
            align-items: center;
        }
        
        .btn-small {
            padding: 6px 12px;
            font-size: 12px;
        }
        
        .btn-danger {
            background: var(--accent-color);
        }
        
        .btn-danger:hover {
            background: #c0392b;
        }
        
        @media (max-width: 768px) {
            .admin-container {
                flex-direction: column;
            }
            
            .admin-nav {
                width: 100%;
                height: auto;
                position: relative;
            }
            
            .admin-main {
                margin-left: 0;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <nav class="admin-nav">
            <h2>🎛️ 后台管理</h2>
            <a href="/admin/dashboard">📊 Dashboard</a>
            <a href="/admin/articles">📝 文章管理</a>
            <a href="/admin/tags">🏷️ 标签管理</a>
            <form action="/admin/logout" method="post" style="margin-top: 30px;">
                <button type="submit" style="width: 100%; background: rgba(231, 76, 60, 0.8); color: white; border: none; padding: 12px; border-radius: 8px; cursor: pointer; font-weight: 500;">
                    🚪 登出
                </button>
            </form>
        </nav>

        <main class="admin-main">
            {% block content %}{% endblock %}
        </main>
    </div>
</body>
</html>