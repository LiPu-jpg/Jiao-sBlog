{% extends "base" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <h1>{{ title }}</h1>
    <div class="article-content">
        <!-- 渲染转换后的 HTML -->
        {{ article_html | safe }}
    </div>
{% endblock %}
